# rbd_results.py -- downloads the workbooks from racing-bet-data.com
requests>=2.31
beautifulsoup4>=4.12

# rbd_import.py -- loads the workbooks into DuckDB. Reads .xlsx natively via
# the bundled excel extension, so no openpyxl/pandas is needed at load time.
duckdb>=1.1

# rbd_web.py -- the table browser
fastapi>=0.110
uvicorn[standard]>=0.27

# rbd_chat.py -- the AI panel on the Races tab. Only Anthropic has an SDK here:
# ChatGPT and Gemini are called over their REST APIs with `requests`, which is
# already a dependency, so they add nothing to install.
anthropic>=1.5
