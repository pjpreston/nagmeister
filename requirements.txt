# rbd_import.py -- loads the workbooks into DuckDB. Reads .xlsx natively via
# the bundled excel extension, so no openpyxl/pandas is needed at load time.
duckdb>=1.1

# rbd_web.py -- the table browser
fastapi>=0.110
uvicorn[standard]>=0.27
