# downloaded race data -- hundreds of MB of xlsx, re-fetchable with rbd_results.py
data/

# interrupted downloads
*.part

# the DuckDB database -- rebuildable from the workbooks via rbd_import.py
*.duckdb
*.duckdb.wal

.venv/

# nag.sh runtime state
.nag.pid
.nag.log

__pycache__/
*.py[cod]

# local-only agent permissions
.claude/settings.local.json
